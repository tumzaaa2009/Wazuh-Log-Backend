{
  "name": "wazuh-api-backend",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun --env-file=.env.dev run --watch src/index.ts",
    "production": "bun --env-file=.env.production run src/index.ts"
  },
  "dependencies": {
    "elysia": "latest"
  },
  "devDependencies": {
    "@types/bun": "^1.3.2",
    "@types/node": "^24.10.1",
    "bun-types": "latest"
  },
  "module": "src/index.js"
}
